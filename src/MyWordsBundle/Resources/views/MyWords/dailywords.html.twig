{% extends "MyWordsBundle::layout.html.twig" %}

{% block title_page %}My Words{% endblock %}

{% block mywords_body %}
  <p>Cet espace est dédié à votre imagination. Il vous permet de saisir votre texte et possède un compteur de mots pour garder un oeil sur votre progression. Vos écrits sont sauvegardés pour vos yeux, et vos yeux seulement. Vous pourrez les retrouver bientôt dans <a href="{{ path('my_stats_daily') }}">votre espace statistiques</a>.</p>

  <div id="daily_words_wrapper" class="content_wrapper">
    <div id="words_header">
      <input type="hidden" id="todays_goal" value="{{ todays_goal }}">
      <span id="word_counter"{% if word_count >= todays_goal and word_count > 0 %} class="quota_reached" title="Félicitations ! Vous avez atteint votre quota journalier. Mais vous pouvez continuer sur votre lancée si vous voulez."{% endif %}>
        {{ word_count }}
      </span>
      <span id="progression_status"></span>
      <button id="focus_me" title="mode focus"></button>
      {# <button id="save_me" title="sauvegarder"></button> #}
    </div>
    <form>
      {# <button id="test_button">Je suis un bouton</button> #}
      <textarea id="my_daily_words">{{ saved_words }}</textarea>
    </form>
  </div>
{% endblock %}
