{% extends "WordWarsBundle::layout.html.twig" %}

{% block title_page %}Posez les stylos !{% endblock %}

{% block wordwars_body %}
  <div id="mywordwar_wrapper" class="content_wrapper">
    <p>Pas d'inquiétude, votre texte est bien sauvegardé ! Vous pouvez le retrouver, avec le reste de vos écrits, <a href="{{ path('my_stats_archives') }}">dans vos archives</a>.</p>
    <p>La WW est terminée ! Voici les résultats :</p>

    <ul id="ww_results_wrapper" class="col-lg-offset-2 col-md-7">
      {% for result in all_ww %}
        <li class="result_row row{% if true == result.is_me %} my_result{% endif %}">
          <div class="avatar col-sm-2">
            {% if result.avatar %}<img src="{{ asset(result.avatar) }}" />{% endif %}
          </div>
          <div class="pseudo col-sm-6">{{ result.username }}</div>
          <div class="word_count col-sm-4">{{ result.word_count }}</div>
        </li>
      {% endfor %}
    </ul>
  </div>
{% endblock %}
