admin_homepage:
    path:     /admin_old
    defaults: { _controller: AdminBundle:Admin:index }

admin_users:
    path:     /admin_old/users/{page}
    defaults:
        _controller: AdminBundle:Admin:users
        page:        0
    requirements:
        page: \d+
